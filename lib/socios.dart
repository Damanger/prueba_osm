import 'package:flutter/material.dart';

class SociosPage extends StatelessWidget {
  const SociosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Estás en la pestaña Socios'),
    );
  }
}
